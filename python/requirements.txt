# APE v1.0.1 - Agent Policy Engine
# Core dependencies

# YAML parsing for policy files
pyyaml>=6.0

# JSON Schema validation for action parameters and data structures
jsonschema>=4.17.0

# =============================================================================
# Development dependencies (optional)
# Install with: pip install -r requirements.txt -r requirements-dev.txt
# Or: pip install agent-policy-engine[dev]
# =============================================================================
# See requirements-dev.txt for development dependencies
